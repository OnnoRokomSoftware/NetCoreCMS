@using NetCoreCMS.Framework.Utility
@model NccComment

@{
    Layout = Constants.AdminLayoutName;
    var mainName = "Comments";
    var controllerName = "CommentsAuthor";
    Title = mainName + " Delete";
    SubTitle = "Delete " + mainName.ToLower();
}
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            @*<div class="panel-heading">
                @SubTitle
                <div class="pull-right"><a asp-controller="CmsPage" asp-action="Index" class="btn btn-outline btn-primary btn-xs">Back to Manage</a></div> 
            </div>*@
            <div class="panel-body">
                <div class="row">
                    <form id="deleteForm" class="form-horizontal" asp-controller="@controllerName" asp-action="Delete" method="post">

                        <div class="col-sm-8 pull-left">
                            <h3>Are you sure you want to delete <strong>@Model.Name</strong> ?</h3>
                            <div class="col-md-12">
                                <input type="hidden" id="Id" name="Id" value="@Model.Id" />
                                <a class="btn btn-default" asp-action="Manage">No</a>
                                <button id="delete" class="btn btn-danger" type="submit">Delete</button>
                            </div>                            
                        </div>
                        
                    </form>

                </div>
                <!-- /.row (nested) -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
</div>
@section Scripts{
    
}
